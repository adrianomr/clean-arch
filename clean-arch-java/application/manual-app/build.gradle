apply plugin: 'java-library'

ext.moduleName = 'org.example.main'

dependencies {
    implementation(
            project(':config'),
            project(':domain'),
            project(':usecase'),
    )
}